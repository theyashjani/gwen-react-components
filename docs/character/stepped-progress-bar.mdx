---
name: Stepped Progress Bar
route: /character/stepped-progress-bar
menu: Character
edit: false
---

import { Playground, Props } from "docz"
import { StepProgressBar } from "../../src"

# Stepped Progress Bar Component

Use this component to ....

<Playground>
	<div style={{ display: "flex", flexDirection: "column", height: "200px", justifyContent: "space-between" }}>
		<StepProgressBar currentStepNumber={4} currentStepProgress={2} currentStepTotal={5} icon="circle" />
		<StepProgressBar currentStepNumber={2} maxStep={10} currentStepProgress={40} currentStepTotal={200} icon="triangle" />
		<StepProgressBar
			currentStepNumber={5}
			maxStep={10}
			currentStepProgress={0}
			currentStepTotal={5}
			icon="square"
			progressColor={"#4B0082"}
			backgroundColor={"#D8BFD8"}
		/>
		<StepProgressBar currentStepNumber={4} maxStep={8} currentStepProgress={2} currentStepTotal={5} icon="star" />
	</div>
</Playground>

<Playground>
	{() => {
		const [state, setState] = React.useState({ step: 1, progress: 0 })
		return (
			<React.Fragment>
				<button
					onClick={() =>
						setState((s) => {
							const p = Math.floor(Math.random() * 10)
							return { progress: p, step: p < s.progress ? s.step + 1 : s.step }
						})
					}
				>
					Gain Experience
				</button>
				<StepProgressBar currentStepNumber={state.step} maxStep={10} currentStepProgress={state.progress} currentStepTotal={10} icon="circle" />
			</React.Fragment>
		)
	}}
</Playground>

<br></br>

<Props of={StepProgressBar} />
